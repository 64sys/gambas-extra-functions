# Gambas Form File 3.0

{ Form Form
  MoveScaled(0,0,64,64)
  Background = Color.Background
  { Panel1 Panel
    MoveScaled(2,1,60,61)
    Background = Color.Background
    { btnAddressIP Button
      MoveScaled(3,3,16,4)
      Text = ("AddresIP")
    }
    { btnLANIP Button
      MoveScaled(3,8,16,4)
      Text = ("LAN IP")
    }
  }
}
